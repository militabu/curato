import { render, screen, fireEvent } from '../test-utils';
import App from '../../App'

test('when cicked on the album icon the MainScreen color should change', () =>{
    //render the main component
    render(<App/>);
    
    
    //get the nav element // the icon 

    //get the mainscreen element for the background color? 

    //fire event -> for the button that is clicked(nav element) fireEvent.click

    //fire event -> for the color change? fireEvent.change

})


